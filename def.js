function defHandler(com, def) {
    if(Array.isArray(def) && def.length == 0) {
	return '';
    } else if(Array.isArray(def) && def.length !=0 ) {
	return def.map(elementh=> com+ ' ' + element).join(' ');
    } else if (def=='false' {
	return '';
    } else if (def == 'true' (return com;} if (def ==
																											      []) {return '';} else {return com + def;}}
